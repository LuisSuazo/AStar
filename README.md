# AStar

Version de Astar desarrollado en C++.

## Ejecución

./$(BDIR)/$(TARGET) "coordenadas" "arcos" "nodo Inicio" "nodo Fin"

En el makefile hay un ejemplo

Los archivos de entrada deben ser iguales a los que hay en la carpeta input.
